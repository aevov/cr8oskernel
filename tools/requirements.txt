# cr8OS Model Import Tools - Requirements

# Core dependencies
transformers>=4.35.0
torch>=2.0.0
numpy>=1.24.0
safetensors>=0.4.0

# Optional for faster downloads
huggingface_hub>=0.19.0
accelerate>=0.24.0

# For API server
# (uses stdlib http.server, no extra deps needed)
